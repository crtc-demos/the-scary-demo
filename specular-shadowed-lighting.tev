stage 0:
  tevprev.rgb = colour0;
  tevprev.a = texmap0[texcoord0];

stage 1:
  tevprev.rgb = clamp (cprev + colour1);
  tevprev.a = (aprev > texmap1[texcoord1].r) ? 1 : 0;

stage 2:
  tevprev.rgb = mix (cprev, c0, aprev);
