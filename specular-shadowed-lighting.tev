stage 0: tevprev.rgb = colour0;
stage 1: tevreg1.rgb = clamp (cprev + colour1);
stage 2: tevprev.rgb = texmap0[texcoord0];
stage 3: tevprev.rgb = (texmap1[texcoord1].r > cprev.r) ? 1 : 0;
stage 4: tevprev.rgb = mix (c0, c1, cprev);
